Scope: unify material across map controls, city, comparison, search/results, settings, language, explanation, story and tooltip surfaces. Mobile touch-only experience with readable opaque tint, minimum44px primary targets, explicit expandable indicator picker, safe-area and scroll containment. Preserve map/data changes. No publication.

Acceptance: real touch actions reach/close every menu on WebKit phone and small-phone, desktop regression; all major surfaces use shared tint/edge/blur; inspected captures over bright map; no viewport overflow or blocked close targets; mobile picker closes after choice, source details remain reachable, dialogs scroll without trapping controls. Keyboard and reduced motion retained.

Implementation: shared CSS tokens (89% tint desktop,96% mobile), subtle specular edges, child controls and focus/press states. Mobile filter grid toggled by48px button, collapses after selection; sources use a44px info disclosure. Native comparison dialog and established city sheet reused.

Verification: tools/qa/glass-touch.mjs PASS on WebKit iPhone15Pro, WebKit iPhoneSE and desktop Chromium Retina, using taps throughout navigation; browser input fill stands in for virtual keyboard typing. Captures outputs/terra-glass inspected. Keyboard focus restore and native CDP touch-swipe scrolling independently checked on small mobile viewport: comparison scrolls, sticky close remains reachable, city returns after close. Background city is hidden while comparison/story is open to prevent text ghosting. All seven map layers passed map-evolution.mjs on desktop and mobile; static checks 8/8, diff check clean. Independent read-only review confirmed focus fix with no remaining blocker. No physical-device or deployment claim. Spend0, concurrency2.
